      /* >>PRE-COMPILER<<                                                 */
      /*   >>CRTCMD<<  CRTCLMOD MODULE(&LI/&OB) SRCFILE(&SL/&SF) +        */
      /*                 SRCMBR(&SM);                                     */
      /*   >>IMPORTANT<<                                                  */
      /*     >>PARM<<  OPTION(*EVENTF);                                   */
      /*     >>PARM<<  DBGVIEW(*ALL);                                     */
      /*   >>END-IMPORTANT<<                                              */
      /*   >>EXECUTE<<                                                    */
      /* >>END-PRE-COMPILER<<                                             */

             PGM



             CHGVAR     VAR(&MAILUSERS) +
                          VALUE('(''odrubigny.externe@ista.fr'' *PRI)')

             ADDLIBLE LAUNCHER

             LNCTOXLS   TOXLS(*NEW) FROMFILE(QTEMP/ERROLIST) +
                          OUTPUT(*) SHOWDOC(*YES)

             DLTF QTEMP/ERROLIST

             ENDPGM
